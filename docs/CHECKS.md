# Checks

## Команды

```bash
npm install
npm run check
npm test
npm run build
git diff --check
```

## Локальная проверка

- Запустить `npm run dev`.
- Открыть локальный адрес Vite.
- Проверить быстрый режим: расклад → тип вопроса → позиция → карта → ориентация.
- Проверить режим «Собрать расклад»: выбор карты закрепляет её в активной позиции и переводит активность дальше.
- Проверить, что «Изменить» позволяет заменить карту, а обычный выбор не перезаписывает заполненную позицию.
- Проверить, что смена типа вопроса сохраняет карты и пересчитывает трактовки.
- Проверить, что смена расклада с выбранными картами показывает подтверждение очистки.
- Проверить, что справочник раскрывается вертикально.

## Мобильная ширина

- Проверить ширину около 390px.
- Убедиться, что нет горизонтальной прокрутки.
- Проверить, что sticky-блок результата не перекрывает элементы.
- Проверить, что кнопки «Изменить», «Очистить» и «Очистить расклад» остаются доступными.

## GitHub Pages

- Workflow `Deploy GitHub Pages` запускается после push в `main`.
- Pages URL: https://kostgame.github.io/arcana-navigator/
- Фактическая публикация проверяется после merge в `main`.
